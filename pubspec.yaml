name: login_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  firebase_core: ^3.13.0
  flutter:
    sdk: flutter
  mask_text_input_formatter: ^2.0.0 # Dependência para máscaras em campos de cadastro
  sqflite: ^2.4.1
  firebase_auth: ^5.5.2  # Dependência para autenticação
  cloud_firestore: ^5.6.6
  google_fonts: ^6.2.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # Ou a versão mais recente

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
