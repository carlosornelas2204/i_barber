name: login_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  firebase_core: ^3.8.1
  flutter:
    sdk: flutter
  sqflite: ^2.4.1
  firebase_auth: ^5.3.4  # Dependência para autenticação
  cloud_firestore: ^5.5.1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
